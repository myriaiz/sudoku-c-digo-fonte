#include <algorithm> //para sort
#include <chrono>
#include <ctime>
#include <ctype.h> //para o isdigit
#include <fstream>
#include <iostream>
#include <stdlib.h>
#include <string> //substr e stoi
#include <vector>

using namespace std;
using namespace std::chrono;

const string linha;


int tira(const string linha);
